# ── 코어 ─────────────────────────────────────────────────────────
numpy
pandas
opencv-python
gradio

# ── 검출 / 추적 ──────────────────────────────────────────────────
ultralytics            # YOLOv8n + ByteTrack (가중치는 첫 실행 시 자동 다운로드)

# ── 세그멘테이션 (GPU 권장, 없으면 bbox 모드로 자동 fallback) ────
# torch / torchvision 은 CUDA 버전에 맞춰 별도 설치를 권장:
#   https://pytorch.org/get-started/locally/
torch
torchvision
sam2                   # SAM2 (facebookresearch/segment-anything-2)

# ── 선택: 원본 오디오 재결합 ─────────────────────────────────────
# ffmpeg 는 pip 패키지가 아니라 시스템에 설치되어 있어야 함 (없으면 무음 결과)
